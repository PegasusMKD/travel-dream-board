version: "2"
sql:
  - schema: "./sql/migrations/"
    queries: "./sql/queries/"
    engine: "postgresql"
    gen:
      go:
        package: "db"
        out: "./internal/db"
        sql_package: "pgx/v5" 
        emit_json_tags: false
        emit_pointers_for_null_types: true
